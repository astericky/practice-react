import React, { PropTypes } from 'react';

const About = (props) => {
    return (
        <div>
            <h3>About</h3>
            <p>Welcome to the about page.</p>
        </div>
    );
};

export default About;
